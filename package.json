{
  "name": "freelance-leads-dashboard-rynell",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:build && npm run test:preview",
    "test:build": "vite build",
    "test:preview": "vite build && (vite preview --strictPort --port 4173 --host 127.0.0.1 & preview_pid=$!; sleep 2; kill $preview_pid)"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
